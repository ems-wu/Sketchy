package Sketchy;

/**
 * This is my SketchyAction enum that sets an enum for each button and colorpicker
 * action. 
 */
public enum SketchyAction {DRAW_LINE, SELECT_SHAPE, DRAW_RECT, DRAW_ELLIPSE, COLOR_PICKER, NULL};

